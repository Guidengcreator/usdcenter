{
	email {$ACME_EMAIL}
}

{$SITE_DOMAIN} {
	root * /srv
	encode zstd gzip

	route {
		handle /api/* {
			reverse_proxy backend:3000
		}

		handle {
			try_files {path} /index.html
			file_server
		}
	}
}
